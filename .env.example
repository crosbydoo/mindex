# Copy to .env.local and adjust:
#   cp .env.example .env.local

# Leave empty in local dev to use Vite proxy (avoids CORS).
# For production builds, set the public API origin:
# VITE_API_BASE_URL=https://mindex-api.duckdns.org
VITE_API_BASE_URL=
